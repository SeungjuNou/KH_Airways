<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="webcheck">

	<select id="checkinList" resultType="hashmap">
		<![CDATA[
			SELECT *  
			FROM
			    WEBCHECK
			ORDER BY BP_NO DESC
		]]>
	</select>

	<!-- <insert id="insertWebcheck" parameterType="hashmap" useGeneratedKeys="true" keyProperty="BP_NO">
		<selectKey keyProperty="BP_NO" resultType="string" order="BEFORE">
			SELECT SEQ_BP_NO.NEXTVAL FROM DUAL
		</selectKey>
		<![CDATA[
			
			INSERT INTO WEBCHECK
			(
				BP_NO,
			    BOOK_NO, 
			    TICKET_NO, 
			    DEP_CODE, 
			    ARR_CODE, 
			    KO_NAME,
			    EN_NAME,
			    PP_NO,
			    PP_EXP,
			    PP_BIRTH
			)
			VALUES
			(
				SEQ_BP_NO.NEXTVAL, 
			    #{BOO_NO}, 
			    #{TICKET_NO}, 
			    #{DEP_CODE},
			    #{ARR_CODE}, 
			    #{KO_NAME},
			    #{EN_NAME},
			    #{PP_NO},
			    #{PP_EXP},
			    #{PP_BIRTH}
			)
		]]>
	</insert> -->
	
	<!-- Book_SQL.XML	
	<select id="viewBook" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT * 
			FROM
				BOOK
			WHERE
				BOOK_NO = #{BOOK_NO}	
		]]>
	</select> -->

</mapper>